import React from "react";
import {
  View,
  Text,
  Image,
  ImageBackground,
  TouchableOpacity,
} from "react-native";

import { SIZES, COLORS, FONTS, icons } from "../constants";

const HorizontalBooksCard = () => {
  return (
    <TouchableOpacity
      style={{
        flexDirection: "row",
        ...containerStyle,
      }} 
    >
      <Text></Text>
    </TouchableOpacity>
  );
};

export default HorizontalBooksCard;
